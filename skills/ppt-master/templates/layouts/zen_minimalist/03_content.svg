<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1280 720" width="1280" height="720">
  <defs>
    <filter id="softShadow" x="-15%" y="-15%" width="140%" height="140%">
      <feGaussianBlur in="SourceAlpha" stdDeviation="6"/>
      <feOffset dx="0" dy="3" result="offsetBlur"/>
      <feFlood flood-color="#000000" flood-opacity="0.05" result="shadowColor"/>
      <feComposite in="shadowColor" in2="offsetBlur" operator="in" result="shadow"/>
      <feMerge>
        <feMergeNode in="shadow"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Background -->
  <rect width="1280" height="720" fill="#FFFFFF"/>

  <!-- Top centered page title -->
  <text x="640" y="104" text-anchor="middle" font-size="36" font-weight="normal" fill="#4a4a48" font-family="SF Pro Display, PingFang SC, Microsoft YaHei, sans-serif">
    <tspan>{{PAGE_TITLE}}</tspan>
  </text>

  <!-- Small accent dot -->
  <circle cx="640" cy="144" r="6" fill="{{ACCENT_COLOR}}" fill-opacity="0.5"/>

  <!-- Content area - one or two gentle rounded cards go here -->
  <!-- Content starts at y=192, massive spacing around everything -->

  <!-- Footer page number -->
  <text x="640" y="690" text-anchor="middle" font-size="10" fill="#adb5bd" font-family="SF Pro Display, PingFang SC, Microsoft YaHei, sans-serif">
    <tspan>{{PAGE_NUM}}</tspan>
  </text>
</svg>
